<% if @success %>
  $('.favorite_link').attr('href', '<%= favorite_path(@favorite, favorable_id: @favorable_id, favorable_type: @favorable_type) %>').data('method', 'delete').text('<%= Spree.t(:remove_favorite) %>')
<% end %>
